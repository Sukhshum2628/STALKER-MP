# Documentation Images

This folder holds diagrams and illustrations referenced by the top-level `README.md`.

Placeholders are listed below; replace them with final exported diagrams (PNG/SVG) as
they become available. Keep filenames stable so README references do not break.

| File | Purpose |
|---|---|
| `architecture-overview.png` | High-level module/boundary diagram (engine ↔ adapters ↔ subsystems). |
| `frame-tick-order.png` | Deterministic per-frame `tick_order` pipeline (100 → 900). |
| `snapshot-dataflow.png` | Sprint-008 snapshot build → publish → consume flow. |
| `sprint-roadmap.png` | Sprint roadmap / milestone timeline. |

Until final art is exported, the README also embeds equivalent Mermaid diagrams
(rendered inline by GitHub) so the documentation is complete without binary assets.
